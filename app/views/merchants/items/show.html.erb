<table class='table' id='item-table'>
  <thead>
    <tr>
      <th>Item Name</th>
      <th>Unit Price</th>
      <th>Item Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @item.name %></td>
      <td>$<%= @item.convert_unit_price_to_dollars %></td>
      <td><%= @item.description %></td>
    </tr>
  </tbody>
</table>
<%= link_to 'Update', edit_merchant_item_path(@merchant, @item), class:'btn btn-primary'%>